version: "2.4"

services:
  app:
    image: sliderpilerdotcom:1.6.1
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development